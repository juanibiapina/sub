#!/usr/bin/env bash
# Summary: Echo arguments 2
# Help: This is a complete test script with documentation.
#
# The help section can span multiple lines.


set -e

# Provide completions
if [ "$1" = "--complete" ]; then
  echo comp1
  echo comp2
  exit
fi

echo "$@"
